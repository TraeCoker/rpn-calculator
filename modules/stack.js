// Stack module to export the linked list Stack class
//
// To build:
// -Stack class with
//   -constructor
//   -push()
//   -pop()
//   -clear()

// single unit of data on the stack
class Node {
    constructor(data) {
        this.data = data
        this.next = null
    }
}
